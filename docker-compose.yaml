services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.build
    ports: 
    - "8000:8000"
  
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.build
    ports:
      - "3000:3000"
    depends_on:
      - backend