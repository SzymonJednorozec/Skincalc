FROM python:3.12-alpine
RUN apk add --no-cache gcc musl-dev libffi-dev

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -e . \
    && python -m pytest

EXPOSE 8000

CMD sh -c "python seed_markets.py && uvicorn main:app --host 0.0.0.0 --port 8000"